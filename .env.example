# Copy this file to .env and fill in your values
# .env is gitignored — never commit real keys

OPENAI_API_KEY=sk-...      # required if using vlm_provider/llm_provider=openai
GROQ_API_KEY=gsk_...      # required if using vlm_provider/llm_provider=groq (free at console.groq.com)
